# 🔍 능력단위 검색 기준 가이드

## 📋 검색 가능한 필터 목록

능력단위 검색 API(`/api/ability-units`)에서 사용할 수 있는 모든 검색 기준입니다.

---

## 1️⃣ 산업분야 (Industry)

**파라미터**: `industry`

**검색 기준**: `ncs_main.major_category_name` (대분류명)

**검색 방식**: 정확 일치 (`=`)

**예시**:
```
GET /api/ability-units?industry=제조업
GET /api/ability-units?industry=서비스업
```

**설명**: NCS 대분류명과 정확히 일치하는 능력단위를 검색합니다.

---

## 2️⃣ 부서 (Department)

**파라미터**: `department`

**검색 기준**: `ncs_main.sub_category_name` (소분류명)

**검색 방식**: 부분 일치 (`ILIKE %값%`)

**예시**:
```
GET /api/ability-units?department=품질관리
GET /api/ability-units?department=생산
```

**설명**: 소분류명에 해당 키워드가 포함된 능력단위를 검색합니다.

---

## 3️⃣ 직무군 (Job Category)

**파라미터**: `jobCategory`

**검색 기준**: `ncs_main.sub_category_name` (소분류명)

**검색 방식**: 정확 일치 (`=`)

**예시**:
```
GET /api/ability-units?jobCategory=품질관리
```

**설명**: `department`와 동일한 필드를 사용하지만, 정확 일치 방식으로 검색합니다.

---

## 4️⃣ 직무 제목 (Job Title)

**파라미터**: `jobTitle`

**검색 기준**: `ncs_main.unit_name` (능력단위명)

**검색 방식**: 부분 일치 (`ILIKE %값%`)

**예시**:
```
GET /api/ability-units?jobTitle=품질관리사
GET /api/ability-units?jobTitle=생산관리
```

**설명**: 능력단위명에 해당 키워드가 포함된 능력단위를 검색합니다.

---

## 5️⃣ 중분류 (Middle Category)

**파라미터**: `middle`

**검색 기준**: `ncs_main.middle_category_name` (중분류명)

**검색 방식**: 정확 일치 (`=`)

**예시**:
```
GET /api/ability-units?middle=경영·회계·사무
```

---

## 6️⃣ 소분류 (Small Category)

**파라미터**: `small`

**검색 기준**: `ncs_main.small_category_name` (소분류명)

**검색 방식**: 정확 일치 (`=`)

**예시**:
```
GET /api/ability-units?small=인사관리
```

---

## 7️⃣ 능력단위 레벨 (Level)

**파라미터**: `level`

**검색 기준**: `ncs_main.unit_level` (능력단위 레벨)

**검색 방식**: 정확 일치 (`=`)

**예시**:
```
GET /api/ability-units?level=3
GET /api/ability-units?level=4
```

**설명**: 능력단위 레벨은 1~5 사이의 숫자입니다.

---

## 8️⃣ 키워드 검색 (Keyword)

**파라미터**: `keyword`

**검색 기준**: 다음 필드들에서 검색
- `ncs_main.small_category_name` (소분류명)
- `ncs_main.unit_name` (능력단위명)
- `ncs_main.unit_element_name` (능력단위 요소명)
- `unit_definition.unit_definition` (능력단위 정의)
- `performance_criteria.performance_criteria` (수행준거)

**검색 방식**: 부분 일치 (`ILIKE %값%`), 여러 키워드는 AND 조건

**예시**:
```
GET /api/ability-units?keyword=급여
GET /api/ability-units?keyword=품질 관리
```

**설명**: 
- 여러 단어를 입력하면 모든 단어가 포함된 결과만 검색됩니다 (AND 조건)
- 대소문자 구분 없이 검색됩니다
- 능력단위명, 요소명, 정의, 수행준거 등 다양한 필드에서 검색됩니다

---

## 9️⃣ 소분류 코드 (Sub Category Code)

**파라미터**: `subCategoryCode`

**검색 기준**: `ncs_main.sub_category_code` (소분류 코드)

**검색 방식**: 정확 일치 (`=`)

**예시**:
```
GET /api/ability-units?subCategoryCode=150302
```

---

## 🔟 세분류 코드 (Small Category Code)

**파라미터**: `smallCategoryCode`

**검색 기준**: `ncs_main.unit_code`의 앞 8자리

**검색 방식**: 정확 일치 (SUBSTRING 비교)

**예시**:
```
GET /api/ability-units?smallCategoryCode=15030202
```

**설명**: 능력단위 코드의 앞 8자리가 일치하는 능력단위를 검색합니다.

---

## 📄 페이지네이션

**파라미터**: 
- `page`: 페이지 번호 (기본값: 1)
- `limit`: 페이지당 결과 수 (기본값: 20)

**예시**:
```
GET /api/ability-units?page=2&limit=10
```

---

## 🔄 검색 조건 조합

여러 검색 조건을 동시에 사용할 수 있습니다. 모든 조건은 **AND** 조건으로 결합됩니다.

**예시**:
```
# 산업분야와 부서로 검색
GET /api/ability-units?industry=제조업&department=품질관리

# 산업분야, 부서, 레벨로 검색
GET /api/ability-units?industry=제조업&department=품질관리&level=3

# 키워드와 산업분야로 검색
GET /api/ability-units?keyword=급여&industry=서비스업
```

---

## 📊 검색 우선순위 및 정렬

### 키워드 검색이 있는 경우:
1. 키워드 매칭 개수 (많을수록 우선)
2. 능력단위 코드 순서

### 키워드 검색이 없는 경우:
1. 능력단위 코드 순서

---

## 🎯 검색 기준 요약표

| 파라미터 | 검색 필드 | 검색 방식 | 예시 값 |
|---------|----------|----------|---------|
| `industry` | `major_category_name` | 정확 일치 | `제조업` |
| `department` | `sub_category_name` | 부분 일치 | `품질관리` |
| `jobCategory` | `sub_category_name` | 정확 일치 | `품질관리` |
| `jobTitle` | `unit_name` | 부분 일치 | `품질관리사` |
| `middle` | `middle_category_name` | 정확 일치 | `경영·회계·사무` |
| `small` | `small_category_name` | 정확 일치 | `인사관리` |
| `level` | `unit_level` | 정확 일치 | `3` |
| `keyword` | 여러 필드 | 부분 일치 (AND) | `급여 관리` |
| `subCategoryCode` | `sub_category_code` | 정확 일치 | `150302` |
| `smallCategoryCode` | `unit_code` (앞 8자리) | 정확 일치 | `15030202` |

---

## 💡 검색 팁

1. **키워드 검색**: 가장 넓은 범위로 검색할 수 있습니다. 여러 필드에서 동시에 검색됩니다.

2. **정확한 검색**: `industry`, `jobCategory`, `middle`, `small` 등은 정확 일치이므로, 정확한 값이 필요합니다.

3. **부분 검색**: `department`, `jobTitle`, `keyword`는 부분 일치이므로, 키워드만 입력해도 검색됩니다.

4. **조합 검색**: 여러 조건을 조합하면 더 정확한 결과를 얻을 수 있습니다.

5. **페이지네이션**: 많은 결과가 예상될 때는 `limit`을 조정하여 페이지당 결과 수를 변경할 수 있습니다.

---

## 🔗 관련 문서

- [API 문서](./API_DOCUMENTATION.md)
- [데이터베이스 스키마](../database/create_tables.sql)
