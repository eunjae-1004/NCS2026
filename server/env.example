# 백엔드 환경 변수 예제
# 이 파일을 복사하여 server/.env 파일을 생성하세요

# PostgreSQL 데이터베이스 연결 설정
# 방법 1: DATABASE_URL 사용 (권장, Railway 등에서 제공)
DATABASE_URL=postgresql://user:password@host:port/database

# 방법 2: 개별 변수 사용 (DATABASE_URL이 없을 때)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=NCS_2026
# DB_USER=postgres
# DB_PASSWORD=your_password_here

# 서버 포트
PORT=3000

# Node.js 환경 (development | production)
NODE_ENV=development

# CORS 허용 도메인 (쉼표로 구분)
# 예: ALLOWED_ORIGINS=https://app.vercel.app,http://localhost:5173
ALLOWED_ORIGINS=https://ncssearch2026.vercel.app,http://localhost:5173,http://localhost:3000

